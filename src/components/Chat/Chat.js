import Messages from "./Messages/Messages";
import ChatForm from "./ChatForm/ChatForm";

const Chat = () => {
  return (
    <section>
      <Messages />
      <ChatForm />
    </section>
  );
};

export default Chat;
